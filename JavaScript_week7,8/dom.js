let inputfield = document.getElementById("input")
let outputfield = document.getElementById("ai-dev")

inputfield.addEventListener("input", () => {
    let collectedData = inputfield.value.trim()
    outputfield.textContent = collectedData //to turn static data to dynamic data
    })
    function inputedFn(){

}

let myForm = document.getElementById("my-form")
let men = document.getElementById("men")
let namer = document.getElementById("names-here")

const menName = [];

myForm.addEventListener("submit", (e) => {
    e.preventDefault()
    let collectedData = men.value.trim();
    noisers.push(collectedData);
    menName.forEach(makersOfNoise) => {
        namer.textContent += 

    });
    name.textContent = collectedData;
});